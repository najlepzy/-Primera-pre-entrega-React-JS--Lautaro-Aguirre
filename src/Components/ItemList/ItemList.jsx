/* 
const ItemList = () => {
  return (
    <>
      <h1>Mercado libre</h1>
      <div>
        {products.map((prod) => {
          return (
            <div>
              <h2>{prod.title}</h2>
              <img src={prod.thumbnail} alt={prod.title} />
              <h3>{prod.price}</h3>
            </div>
          );
        })}
      </div>
    </>
  );
};

export default ItemList;
 */